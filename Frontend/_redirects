# API requests are handled by the Cloudflare Pages Function in functions/api/[[path]].js.
# Keep this SPA fallback below the API Function route.

http://www.travellex.tours/* https://travellex.tours/:splat 301!
https://www.travellex.tours/* https://travellex.tours/:splat 301!

/* /index.html 200
